import React, {FC} from 'react';
import {Box, Center} from "@chakra-ui/react";

export const AsyncOnScroll: FC = props => {
    return (
        <Box>
            <Center>Карточки, подгружаемые со скроллом</Center>
        </Box>
    )
};